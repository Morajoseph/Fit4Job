namespace Fit4Job.Enums
{
    // Enum for TrackAttempt status
    public enum AttemptStatus
    {
        [Display(Name = "In Progress")]
        InProgress,

        [Display(Name = "Completed")]
        Completed
    }
}